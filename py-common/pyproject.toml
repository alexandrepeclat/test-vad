[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "py-common"
version = "0.1.0"
dependencies = [
#    "numpy",
    "matplotlib"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["py_common*"]